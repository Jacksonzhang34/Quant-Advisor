# Shared (non-secret placeholders)
NODE_ENV=development

# API
API_PORT=4000
API_BASE_URL=http://localhost:4000

# Postgres
DATABASE_URL=postgres://qa:qa@localhost:5432/quant_advisor

# Redis / BullMQ
REDIS_URL=redis://localhost:6379

# Crypto (32-byte key; generate for real usage)
# Example generation (node): `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`
APP_ENCRYPTION_KEY_BASE64=REPLACE_ME

# Plaid
PLAID_ENV=sandbox
PLAID_CLIENT_ID=REPLACE_ME
PLAID_SECRET=REPLACE_ME

# FMP
FMP_API_KEY=REPLACE_ME


